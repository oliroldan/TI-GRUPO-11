<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('./partials/head') %>

	<body>
		<!-- Header -->
		<% if (locals.user != undefined) { %>
			<%- include('./partials/headerLogueado') %>
		 <% } else { %>
			<%- include('./partials/header') %>
		<% } %>
		<!-- /Header -->

			<!-- product detail -->
			<div class="container products-wrapper">
				<div class="d-flex align-items-center">
					<h2 class="products-title">
						<%= productos.nombre %>
					</h2>
					<p class="product-detail-description">
						<a href="/product/product-add">Editar</a>
					</p>
				</div>
				<div class="product-detail">
					<div class="row">
						<article class="col-12 col-lg-8 product-detail-info">
							<img src="<%= productos.fotoProducto %>" alt="" class="product-detail-img"> <!-- //PORQUE NO CARGA LA FOTO// -->
							<p class="product-detail-description">
								<%= productos.descripcion %>
							</p>
						</article>
						<div class="col-12 col-lg-4">
							<section class="comments">
								<% for( let i=0; i < 3; i++ ) { %>
									<article>
										<img src="<%= usuario.fotoPerfil %>" alt="">  
										<div class="data">
											<p class="user">
												<strong> <a href="/users/profile"></a>
													<%= productos.comentarios[i].nombreUsuario %>
												</strong>
												<%= productos.comentarios[i].texto %> 
											</p>
										</div>
									</article>
									<% } %>
							</section>
							<form class="form" action="/product" method="get">
								<input class="agregar comentario" type="text" value="" placeholder="Agregar comentario">
								<button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button>
							</form>
							<hr>
						</div>
					</div>
				</div>
			</div>
			<!-- /product detail -->

			<!-- footer -->
			<%- include('./partials/footer') %>
			<!-- /footer -->

	</body>

</html>