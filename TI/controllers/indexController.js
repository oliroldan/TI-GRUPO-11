/* importar info que voy a usar en este modulo */
const index = require('../views'); // todavia no tenemos el db


/* crear el modulo en si */
/* const indexController = {
    index: function(req, res) {
        return res.render(});
    }
}; */

/* exportar el modulo */
module.exports = indexController;